# Database Configuration (MANDATORY)
DB_PASSWORD=
MYSQL_ROOT_PASSWORD=

# Backend Configuration (MANDATORY)
JWT_SECRET=

# Database Configuration (Optional - defaults available in docker-compose)
DB_HOST=mysql
DB_USER=user
DB_NAME=mina_db

# Redis Configuration (Optional - defaults available in docker-compose)
REDIS_URL=redis://redis:6379

# Backend Configuration
PORT=3000

# Cache TTL (in seconds)
CACHE_TTL_ITEMS=3600
CACHE_TTL_SEARCH=300
