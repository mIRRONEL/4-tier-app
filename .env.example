# Database Configuration (MANDATORY)
DB_PASSWORD=tyH5783vj
MYSQL_ROOT_PASSWORD=tyH5783vj

# Backend Configuration (MANDATORY)
JWT_SECRET=

# Database Configuration (Optional - has defaults in docker-compose)
DB_HOST=mysql
DB_USER=user
DB_NAME=mina_db

# Redis Configuration (Optional - has defaults in docker-compose)
REDIS_URL=redis://redis:6379

# Backend/Frontend Configuration
PORT=3000
VITE_API_URL=http://localhost:3000
